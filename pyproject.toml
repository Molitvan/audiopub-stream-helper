[project]
name = "audiopub-stream-helper"
version = "0.1.0"
description = "A small utility to enhance the experience of streaming on Audiopub"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "playsound3>=3.3.1",
    "requests>=2.34.2",
    "sseclient-py>=1.9.0",
]
